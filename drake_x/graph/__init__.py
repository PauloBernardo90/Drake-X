"""Graph query, serialization, and rendering utilities.

This package provides operations over :class:`EvidenceGraph` instances:

- :mod:`.query` — bounded neighborhood extraction + filtering
- :mod:`.context` — graph→AI prompt serializer
- :mod:`.render_ascii` — terminal text renderer for ``drake graph show``
- :mod:`.render_summary` — statistics and cluster summary
"""
